# ERP Web 系统

基于 React + 微前端的企业级 ERP 系统

## 📚 技术栈

- **主框架**: React 18 + TypeScript
- **构建工具**: Vite 5
- **路由**: React Router v6
- **状态管理**: Zustand
- **UI 组件**: Ant Design 5
- **样式方案**: Less + CSS Modules
- **微前端**: Wujie
- **包管理**: pnpm Workspace
- **HTTP 请求**: Axios
- **日期处理**: Day.js

> 详细技术栈说明请查看 [TECH_STACK.md](./TECH_STACK.md)

## 🏗️ 项目结构

```
erp-web/
├── packages/
│   ├── main/              # 主应用（核心团队维护）
│   ├── customer/          # 客户管理子应用（待创建）
│   ├── order/             # 订单管理子应用（待创建）
│   └── ...
├── .github/               # CI/CD 配置
│   ├── CODEOWNERS        # 代码所有权
│   ├── CORE_TEAM         # 核心团队成员
│   ├── scripts/          # 检查脚本
│   └── workflows/        # GitHub Actions
├── pnpm-workspace.yaml
└── package.json
```

## 🚀 快速开始

### 1. 安装依赖

```bash
pnpm install
```

### 2. 启动开发

```bash
# 启动所有应用
pnpm dev

# 只启动主应用
pnpm dev:main
```

访问：http://localhost:3100

### 3. 构建

```bash
pnpm build
```

## 👥 开发规范

### 权限管理

- **主应用** (`packages/main/`): 只有核心团队成员可以修改
- **子应用**: 各自团队负责维护
- 所有修改必须通过 PR 并经过审核

### 提交流程

1. 创建分支: `git checkout -b feature/xxx`
2. 开发并提交: `git commit -m "feat: xxx"`
3. 推送: `git push origin feature/xxx`
4. 创建 PR 并等待审核
5. 通过 CI 检查后合并

### 分支保护

- `main` 分支受保护
- 必须通过 PR 合并
- 需要至少 1 人审核
- 必须通过所有 CI 检查

## 📋 开发指南

### 添加新子应用

```bash
cd packages
pnpm create vite <app-name> --template react-ts
```

### 主应用导出

主应用导出的内容可供子应用使用：

```typescript
// 在子应用中使用
import { DataTable, useFlow } from '@erp/main';
```

## 🔧 核心团队

- @tech-lead - 技术负责人

## 📄 License

MIT

